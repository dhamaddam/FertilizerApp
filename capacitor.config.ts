import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    "appId": "com.iopri.fertilizer",
    "appName": "fertilizerApp",
    "webDir": "www",
    "bundledWebRuntime": false
};

export default config;